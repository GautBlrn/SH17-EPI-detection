# Dépendances de la démonstration Streamlit, version DÉPLOIEMENT.
#
# Trois écarts avec le requirements.txt local, et un seul motif à chaque fois :
# ce qui marche sur un poste ne marche pas dans le conteneur de Streamlit Cloud.
#
# 1. torch en roues CPU. `pip install ultralytics` tire par défaut la variante
#    CUDA sur Linux, environ 2,5 Go, et le build échoue avant la fin. L'index
#    supplémentaire ci-dessous force les roues CPU, qui sont ce dont on a besoin
#    puisque l'hébergement n'a pas de carte graphique.
# 2. opencv-python-headless au lieu d'opencv-python. Le paquet non headless
#    réclame libGL, absente du conteneur, et lève `ImportError: libGL.so.1`.
# 3. Rien d'autre ne change.

--extra-index-url https://download.pytorch.org/whl/cpu

torch
torchvision

streamlit>=1.30
ultralytics>=8.3
opencv-python-headless>=4.8
numpy>=1.24
pandas>=2.0
pillow>=10.0
plotly>=5.18
